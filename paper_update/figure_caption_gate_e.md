# External figure disposition
No external-performance figure was generated because no eligible pairs or model measurements exist. There is no empirical caption to insert. Once a valid evaluation is executed, a paired-difference plot must identify the comparator, manifest matching, seed count and uncertainty procedure; until then, retain the existing LPcode figures only.
